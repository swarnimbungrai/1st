//package servlets;

//import java.util.Enumeration;

//import javax.servlet.annotation.WebServlet;
//import javax.servlet.http.HttpServlet;
//import javax.servlet.http.HttpServletRequest;
//import javax.servlet.http.HttpServletResponse;

//@WebServlet("/anotherServlet")
//public class AnotherServlet extends HttpServlet {
	
	//public void doGet(HttpServletRequest request, HttpServletResponse response) { //it should be doPost if method is post in .html
//		Enumeration<String> parameters = request.getParameterNames(); //enum? stdId,....=element to phase every element we use loop
	//	while(parameters.hasMoreElements()) {
		//	System.out.println(parameters.nextElement());  //prints another element until  it ends
		//}
	//	System.out.println(request.getParameter("StdId")); //
		
		
		
	//}

//}
